# 下一阶段工作计划

## 当前起点

已完成 DEAM 数据入口建设：

- 已获取 205 首音频；
- 已生成 `manifest.csv`；
- 已补齐音频时长和采样率；
- 已生成 1 Hz Valence/Arousal 标签；
- 已完成基础数据校验。

## 后续工作主线

### 1. 统一音频数据

将现有音频统一转换为模型所需的 WAV 格式，并完成音频与标签的基本对应检查。

### 2. 固定数据划分

按照曲目划分训练集、验证集和测试集，避免同一首歌曲或其片段出现在不同数据集内。

### 3. 完成音乐结构分析

使用 All-In-One 为音频生成节拍、边界和乐段结构结果，并保存结构分析版本信息。

### 4. 提取音乐音频特征

使用冻结的 MERT 提取音频表示，并将特征统一到与情绪标签一致的时间尺度。

### 5. 完成时间轴对齐

检查音频、情绪标签、MERT 特征和 All-In-One 结构是否对应同一绝对时间轴。

### 6. 建立情绪预测基线

先实现训练集均值、局部模型和平坦时序模型，确认动态情绪预测流程可以正常训练和评估。

### 7. 建立结构感知模型

实现等长分段模型和自动乐段层级模型，比较音乐结构信息对逐时刻情绪预测的影响。

### 8. 完成实验评估

使用 Valence/Arousal 的 CCC、MAE 和 RMSE 进行曲目级评估，并开展必要的消融和统计比较。

### 9. 整理论文材料

汇总数据处理、模型设置、实验结果、结构质量和研究限制，形成论文中的方法、实验和结论部分。

## 推荐执行顺序

```text
统一 WAV
  → 固定数据划分
  → All-In-One 结构分析
  → MERT 特征提取
  → 时间轴对齐检查
  → 基线模型
  → 结构感知模型
  → 消融与评估
  → 论文整理
```

## 阶段目标

下一阶段首先完成“音频、标签、结构和特征”的统一数据闭环，再进入模型训练。只有基线模型能够稳定运行，才继续进行结构感知模型和论文实验。

以后每次新增、修改或完成任何功能，都必须同步更新 draft.md 文档，记录功能名称、完成状态、修改内容和日期。

# 数据集、人工处理与数据协议

# 数据集、人工处理与数据协议



## 1\. 推荐数据策略



不要把不同数据集的歌曲直接拼接成一张假设存在完整标签的“大数据集”。DEAM 有逐秒情绪但没有可直接假定的人工段落；SALAMI/RWC 主要有结构或节拍但不一定有逐秒情绪。推荐采用：



\- **\*\*主训练数据：DEAM\*\***，提供逐秒 Valence/Arousal；

\- **\*\*结构输入：All\-In\-One 自动生成\*\***，不称作人工真值；

\- **\*\*外部验证：PMEmo\*\***，先核验许可、音频和时间标签；

\- **\*\*结构质量诊断：SALAMI 或小规模人工审计子集\*\***；

\- **\*\*不使用 MusicCaps 作为主情绪监督\*\***，它是文字描述数据，不能直接和 DEAM 逐曲对齐。



## 2\. 可用开源/公开资源



|数据/工具|内容|本项目用途|主要限制|
|---|---|---|---|
|DEAM<br>|1,802 个片段和完整歌曲；逐秒及整曲 VA<br>|主任务训练、验证和测试|没有可直接假定的人工段落标签；版权和下载条款需遵守|
|PMEmo|794 首歌曲、457 名参与者；整曲与每 0\.5 秒动态标签、EDA|外部情绪验证|版权限制下主要提供副歌片段，不是完整歌曲库|
|SALAMI|音乐结构边界、层级结构和重复关系|结构分析器质量参考或独立结构验证|与 DEAM 曲目不完全重合，不能直接产生情绪标签|
|RWC Music Database|多类音乐信息检索标注，包括部分结构/节拍资源|结构或节拍补充评测|通常需要申请，许可不能简单视为完全开放|
|All\-In\-One|节拍、下拍、功能段落边界和标签分析工具|冻结结构输入生成器|自动输出不是人工结构真值|
|MERT|大规模自监督音乐表示|冻结音频特征提取器|模型和音频使用许可需单独核验|



DEAM 官方页：\<https://cvml\.unige\.ch/databases/DEAM/\>  

PMEmo 官方仓库：\<https://github\.com/HuiZhangDB/PMEmo\>  

All\-In\-One：\<https://github\.com/mir\-aidj/all\-in\-one\>  

MERT：\<https://arxiv\.org/abs/2306\.00107\>



## 3\. 主数据选择 DEAM



### 3\.1 DEAM 在本项目中到底用来做什么



可以把 DEAM 理解为本项目的**\*\*情绪答案数据集\*\***：它告诉模型，在歌曲的第 \`t\` 秒，人类标注者认为这段音乐的 Valence（效价）和 Arousal（唤醒度）大约是多少。模型读取音频后，学习预测这两个连续数值。



因此，DEAM 主要承担以下任务：



\- **\*\*训练\*\***：用音频和逐秒 VA 标签训练情绪预测模型；

\- **\*\*验证\*\***：选择模型结构、损失权重、时间延迟和超参数；

\- **\*\*测试\*\***：在从未参与训练的曲目上计算 CCC、MAE 和 RMSE；

\- **\*\*构造情绪轨迹\*\***：把每秒标签按时间排列，形成歌曲的真实情绪变化曲线，与模型预测曲线比较。



它不负责告诉我们“这一秒属于主歌还是副歌”。这个信息由结构分析器或人工结构标注提供。可以用下面的输入—答案关系理解数据分工：



```Plain Text
DEAM：音频 → 每秒 Valence/Arousal 情绪答案
结构标注：音频 → 段落边界和段落类别答案
本项目：音频 + 段落上下文 → 每秒 Valence/Arousal 预测
```



DEAM 官方明确提供逐秒连续 Valence 和 Arousal，能够直接构造：



```Plain Text
音频 → 每秒特征 → 每秒 VA 回归 → CCC/MAE/RMSE
```



这比自行把歌曲情绪文字转换成数值更可靠，也避免大规模人工情绪标注。



### 3\.2 为什么说 DEAM 没有“人工段落真值”



“人工段落真值”指的是：人工听歌后，明确标出 \`0–12\.4 秒是前奏\`、\`12\.4–38\.1 秒是主歌\`、\`38\.1–65\.0 秒是副歌\` 这样的**\*\*段落边界和段落类别标签\*\***。这里的“真值”不是绝对客观真理，而是本实验约定的人工参考答案，用来评估自动结构分析是否准确。



DEAM 的人工标注内容是情绪维度，不是歌曲结构。它提供的是某一时间点的 Valence/Arousal 评分或连续情绪轨迹；不能因为这些标签带有时间戳，就推断 DEAM 也记录了主歌、副歌、桥段等结构边界。对于 DEAM 中的一首歌曲，我们可以知道：



- 人工标注者认为每秒的情绪数值是多少；

- 情绪数值随时间如何变化；

- 在整曲层面，歌曲的总体情绪大致如何；

但不能仅凭 DEAM 的原始标签知道：



- 前奏从哪一秒开始、结束；

- 哪些时间属于主歌、副歌或桥段；

- 哪些段落在歌曲中重复出现；

- 自动分析器给出的边界是否符合人工音乐结构判断。

所以，原文“DEAM 不能被描述为同时提供人工段落真值”的意思是：**\*\*DEAM 可以提供情绪监督，但不能直接作为段落结构的评测答案。\*\*** 这不是说 DEAM 没有人工标注，而是说它的人工标注只覆盖情绪维度，没有覆盖本课题所需的段落结构维度。



### 3\.3 本项目如何补充段落信息



在 DEAM 提供情绪答案的基础上，首阶段使用冻结的 All\-In\-One 对每首 DEAM 音频生成段落边界和段落归属。这样形成：



\- **\*\*监督标签\*\***：DEAM 的逐秒 Valence/Arousal；

\- **\*\*模型输入\*\***：音频秒级特征、当前秒所属的自动段落、段落级池化特征和段落序列上下文；

\- **\*\*结构评估\*\***：默认只把 All\-In\-One 输出视为实验输入，不把它当成人工正确答案；

\- **\*\*有限质量检查\*\***：在预先选定的小规模子集上进行人工结构审计，检查边界是否明显错位，并报告标注者分歧。



如果论文需要严格评估段落识别本身，就必须额外使用 SALAMI 等带结构标注的资源，或自行人工建立数据集。不能把 All\-In\-One 的自动输出反过来当作自己的人工真值，也不能把 DEAM 的情绪标签当作段落标签。



结构信息因此必须是：



1. 冻结 All\-In\-One 的自动结构输出；或

2. 另外构造有限人工结构审计子集；或

3. 仅把结构模型输出作为实验输入，不声称结构识别正确。

## 4\. PMEmo 的位置



PMEmo 论文和官方仓库说明它包含 794 首歌曲、457 名参与者、动态 0\.5 秒情绪标注以及同步皮电信号。它可以回答外部验证问题：模型在另一个标注协议和数据来源上是否仍能预测动态情绪。



但 PMEmo 受版权限制，完整歌曲不随数据集提供，主要是副歌片段、特征和标注。因此：



- 不把 PMEmo 作为首阶段结构建模的必需数据；

- 不假设 DEAM 与 PMEmo 曲目可逐曲合并；

- 只有确认音频获取、许可和时间轴后才运行外部测试。

## 5\. 文件组织和统一字段



### 5\.1 这一节定义的是什么



DEAM 的音频与情绪标签、All\-In\-One 的结构结果，以及训练/测试划分来自不同处理步骤，文件名和格式可能互不相同。这里的“统一字段”不是 DEAM 官方提供的全部字段，而是**\*\*本项目自行建立的数据索引规范\*\***，用于把同一首歌的所有文件关联起来。



可以把 `manifest` 理解成一张“曲目总目录”：每首歌占一行。它不保存完整音频或全部逐秒标签，只记录曲目标识、文件位置、有效时间范围和数据状态。程序先读取 `manifest`，再根据其中的路径找到音频、情绪标签、结构结果和缓存特征。（`manifest` 看成一个数据集的元数据文件）



建议管理以下五类文件：



|文件类型|建议格式|保存内容|在实验中的作用|
|---|---|---|---|
|曲目清单<br>|`manifest.csv` 或 `manifest.parquet`|每首歌的 ID、文件路径、划分和质量状态|所有数据的统一入口|
|音频文件<br>|数据集原格式，或统一 WAV/FLAC|歌曲声音信号|模型输入来源|
|情绪标签<br>|CSV 或 Parquet|时间、Valence、Arousal、有效标记|训练和评价的监督答案|
|结构结果<br>|每曲一个 JSON|自动段落边界、段落类别、分析器版本|段落模型输入，不是人工真值|
|特征缓存<br>|NumPy、PyTorch 或 HDF5|MERT 等模型提取的秒级音频特征|避免每轮训练重复提取特征|



### 5\.2 `manifest` 字段概览\(数据集的元数据文件\)



为每曲建立一行 `manifest`：



```Plain Text
track_id
source_dataset
audio_path
duration_sec
sample_rate
label_path
label_hz
label_start_sec
label_end_sec
structure_path
structure_version
split
license_status
quality_status
exclude_reason
```



各字段含义如下：



|字段|示例|定义和用途|
|---|---|---|
|`track_id`|`deam_0001`<br>|本项目内部唯一曲目编号，用于关联音频、标签、结构和特征；不能为空或重复|
|`source_dataset`<br>|`DEAM`<br>|曲目来源数据集，用于区分标签协议、处理方式和许可|
|`audio_path`<br>|`audio/deam_0001.mp3`<br>|音频文件的项目内相对路径；文件应存在且可解码|
|`duration_sec`<br>|`65.0`<br>|实际可用音频时长，单位为秒；应从音频读取，而不是仅相信文件名或元数据|
|`sample_rate`<br>|`44100`<br>|音频每秒采样点数，单位为 Hz；它和情绪标签频率不是一回事|
|`label_path`<br>|`labels/deam_0001.csv`<br>|音频文件逐秒 Valence/Arousal 标签文件的路径|
|`label_hz`<br>|`1.0`<br>|每秒有多少个情绪标签点；`1.0` 表示每秒一个，`2.0` 表示每 0\.5 秒一个|
|`label_start_sec`<br>|`15.0`|第一条有效情绪标签对应的音频时间；不能默认一定从第 0 秒开始|
|`label_end_sec`<br>|`60.0`|情绪监督覆盖到的结束时间；必须不超过音频时长|
|`structure_path`<br>|`structures/deam_0001.json`<br>|All\-In\-One 自动结构结果路径；缺失时应留空并记录原因，不能伪造结果|
|`structure_version`<br>|`v1.0` 或 Git commit<br>|生成结构结果使用的代码、模型或权重版本，用于复现实验|
|`split`<br>|`train`<br>|数据划分，只允许预先约定的 `train`、`validation`、`test` 等值|
|`license_status`<br>|`verified`<br>|音频和标签的使用、处理及再分发许可状态，可用 `verified`、`restricted`、`unknown`|
|`quality_status`<br>|`ok`<br>|数据质检状态，可用 `ok`、`warning`、`excluded`；它不代表音乐质量高低|
|`exclude_reason`<br>|`label_time_mismatch`<br>|曲目被排除时的机器可读原因；未排除时留空|



一个简化的 `manifest.csv` 示例是：



```Plain Text
track_id,source_dataset,audio_path,duration_sec,sample_rate,label_path,label_hz,label_start_sec,label_end_sec,structure_path,structure_version,split,license_status,quality_status,exclude_reason
deam_0001,DEAM,audio/deam_0001.mp3,65.0,44100,labels/deam_0001.csv,1.0,0.0,65.0,structures/deam_0001.json,all-in-one-v1,test,verified,ok,
```



这表示：`deam_0001` 来自 DEAM，音频长 65 秒，情绪标签每秒一个点，自动结构结果位于对应 JSON 文件，该曲属于测试集并已通过基础质检。



### 5\.3 情绪标签文件



`label_path` 指向的文件保存一首歌的逐时刻情绪答案，而不是把所有标签塞进 `manifest`。统一后的最小字段建议为：



```Plain Text
time_sec,valence,arousal,valid
0.0,0.12,0.43,1
1.0,0.15,0.47,1
2.0,,,0
```



|字段|含义|
|---|---|
|`time_sec`|标签对应的音频时间，单位为秒|
|`valence`|效价数值，表示更偏消极还是积极；具体范围以原数据和预处理记录为准|
|`arousal`|唤醒度数值，表示更偏平静还是激烈|
|`valid`|该时间点是否可用于损失和评价；缺失值或无效点记为 `0`|



原始标签必须保留不覆盖；若进行了插值、归一化或时间延迟修正，应另存处理后版本，并记录处理脚本和参数。



### 5\.4 结构 JSON 文件



结构 JSON 至少包含：



```JSON
{
  "track_id": "deam_0001",
  "analyzer": "all-in-one",
  "version": "release-or-git-commit",
  "audio_duration_sec": 65.0,
  "boundaries_sec": [0.0, 12.4, 38.1, 65.0],
  "segment_labels": ["intro", "verse", "chorus"],
  "status": "ok"
}
```



|字段|含义|
|---|---|
|`track_id`|必须与 `manifest` 中的曲目编号一致|
|`analyzer`|生成结果的结构分析器名称；这里是 All\-In\-One|
|`version`|分析器代码、模型或权重版本；不能只写“最新版本”|
|`audio_duration_sec`<br>|分析器实际处理的音频时长，用于检查是否处理了错误文件或截断音频|
|`boundaries_sec`|按时间递增的段落边界；包含起点 `0.0` 和整曲终点|
|`segment_labels`|每个相邻边界区间的自动段落类别；类别数量应等于边界数量减一|
|`status`|结构生成状态，如 `ok`、`warning`、`failed`|



上例表示三个区间：`0.0–12.4` 秒为前奏，`12.4–38.1` 秒为主歌，`38.1–65.0` 秒为副歌。这里的标签是自动分析结果，不是 DEAM 提供的人工答案。如果分析器只可靠输出边界、不可靠输出功能名称，可以把 `segment_labels` 写成统一的未知值，主实验只使用边界和段落归属。



### 5\.5 推荐目录示例



```Plain Text
data/
├── manifest.csv
├── audio/
│   └── deam_0001.mp3
├── labels_raw/
│   └── deam_0001.csv
├── labels_processed/
│   └── deam_0001.csv
├── structures/
│   └── deam_0001.json
├── features/
│   └── deam_0001.pt
└── splits/
    ├── train.txt
    ├── validation.txt
    └── test.txt
```



`labels_raw` 保存下载后的原始标签，禁止覆盖；`labels_processed` 保存统一时间轴、有效掩码或归一化后的标签；`structures` 保存自动结构；`features` 保存冻结编码器特征；`splits` 保存最终冻结的曲目 ID 列表。



### 5\.6 写入后必须自动检查



数据准备脚本至少应验证：



1. `track_id` 全局唯一，且各文件内部的 ID 与 `manifest` 一致；

2. `audio_path`、`label_path` 和非空的 `structure_path` 都存在；

3. `0 ≤ label_start_sec < label_end_sec ≤ duration_sec`；

4. 情绪标签时间严格递增，Valence/Arousal 数值范围符合当前处理版本；

5. 结构边界严格递增，首边界为 0，末边界不超过音频时长；

6. `len(segment_labels) = len(boundaries_sec) - 1`；

7. `excluded` 曲目具有非空 `exclude_reason`，且训练程序不会加载；

8. 同一曲目或其切片不会同时出现在多个数据划分中。

统一字段的作用是让数据错误能在训练前被发现，而不是增加新的模型特征。首轮实现可以使用 CSV 和 JSON；当数据规模或读取速度成为问题时，再改用 Parquet、HDF5 等格式。



## 6\. 人工需要做



### 6\.1 数据核验



人工抽查音频、标签曲线和自动段落是否明显错位，检查：



- 音频能否播放；

- 标签起止是否对应音频；

- 是否存在大量零长度或极短段；

### 6\.2 结构边界审计



不建议一开始建设大规模人工结构数据。建议从测试集预先抽取 60–100 首，按时长、风格、段落数量分层，由至少两名标注者标记：

- 感知到的主要段落边界时间；

- 边界显著程度；

- 可选功能标签；

- 不确定时间区间；

- 是否不适合 intro/verse/chorus 体系。

标注者不能在看到模型结果后重新标注。



### 6\.3 情绪标签人工处理



DEAM/PMEmo 已经提供情绪标签，人工主要做：



- 检查缺失值；

- 确认数值范围；

- 处理有效掩码；

- 检查时间轴；

- 仅在训练/验证集选择全局反应时延；

- 不为每个测试曲目单独寻找最优延迟。

不应把 DEAM 标签重新主观改写成“快乐/悲伤”。



## 7\. 标注一致性与质量



结构审计采用：



- 多尺度边界 precision/recall/F1；

- 边界匹配距离分布；

- 标注者间一致性；

- 功能标签一致率作为辅助；

- 曲目是否不适合标准流行曲式的比例。

不要预先规定“边界前后固定几秒即正确”。可以报告多尺度结果，并把标注者分歧作为不确定性参考。



## 8\. 时间轴处理



统一到 1 Hz 监督网格：



- 标签原生有效点保留；

- 缺失点使用有效掩码；

- 不在测试集上插值或选择延迟；

- 特征在每个标签秒内按固定规则平均；

- 边界落在秒之间时，采用预先固定的归属规则；

- 保存原始时间和标准化时间，避免不可追溯。

反应时延建议在训练/验证集预注册候选范围，例如 0–5 秒整数候选，由验证集选择一个全局延迟，再冻结到测试集。



## 9\. 划分与泄漏



统计单位是曲目。所有同曲切片必须进入同一划分。划分前检查：



- 同一歌曲的不同片段；

- 同一作品的不同演奏；

- 重混、翻唱和重复上传；

- 艺术家或专辑信息造成的近重复；

- 预训练模型和实验测试集之间可能的音频重叠。

每个模型共享同一 train/validation/test 列表。bootstrap 和显著性检验按曲目进行。



## 11\. 推荐最小数据闭环



### 第一阶段



只使用：



- DEAM 音频和逐秒 VA；

- 冻结 MERT；

- 冻结 All\-In\-One；

- 60–100 首结构人工审计子集。

### 第二阶段



在资源和许可通过后加入：



- PMEmo 外部动态情绪验证；

- SALAMI 结构质量对照；

- 风格分层和跨数据集分析。

如果第一阶段的平坦模型、等长分段模型和段落层级模型无法稳定复现，不进入更多数据和复杂模型。


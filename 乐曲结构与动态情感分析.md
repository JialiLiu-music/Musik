# 乐曲结构与动态情感分析

# 从零跑通乐曲结构与动态情感分析



> 适用环境：Ubuntu 22\.04/24\.04 \+ NVIDIA GPU  
> 
> 课题目标：使用 DEAM 动态 Valence/Arousal（VA）标签训练逐时刻音乐情绪预测模型，并检验 All\-In\-One 自动乐段是否带来额外收益。  
> 
> 文档状态：实验实施手册，2026\-09\-03。上游软件版本变化时应重新验证安装命令。
> 
> 



---



## 1\. 先给结论



这项研究可以做，但不能把“结构分析”和“情绪分析”理解成两个现成模型直接串联：



```Plain Text
DEAM 音频
  ├── MERT（冻结）               -> 秒级音频特征 h_t
  ├── All-In-One（冻结）         -> 自动乐段边界/类别
  └── DEAM 动态人工标注          -> VA 监督标签 y_t

h_t + 当前乐段池化 + 乐段序列上下文
  -> 自己训练的轻量 PyTorch 回归模型
  -> 每秒 Valence/Arousal
```



具体选择如下：



\- **\*\*情绪监督数据集用 DEAM\*\***。它提供随时间变化的 Valence（积极/消极程度）和 Arousal（激烈/平静程度）。

\- **\*\*音频情绪特征用冻结的 MERT\-v1\-95M\*\***。不要首轮端到端微调 MERT。

\- **\*\*结构输入用冻结的 All\-In\-One\*\***。只使用它生成的边界和可选功能标签，不重新训练 All\-In\-One。

\- **\*\*情绪预测器需要自行训练\*\***。首选双向 GRU 基线和“秒级—乐段级”层级模型，而不是先上音乐大语言模型。

\- **\*\*第一篇论文只研究一个问题\*\***：在同一音频表示、同一划分和相近参数量下，自动乐段上下文是否优于平坦时序和等长分段？



DEAM 适合这个任务，但有两个必须纠正的细节：



1\. 官方动态平均标签 CSV 的列实际间隔为 **\*\*500 ms（2 Hz）\*\***，例如 \`sample\_15000ms\`、\`sample\_15500ms\`，不能仅根据网页“per\-second”描述假设原文件已经是 1 Hz。

2. 官方说明删除了最开始 15 秒的不稳定动态标注。因此首个有效时间是 15\.0 秒，不能把第一列错误对齐到音频第 0 秒。

本项目可统一到 1 Hz：将 `[t, t+0.5]` 两个标签取均值，并把结果时间记为 `t+0.5`；音频特征也在相同的一秒区间聚合。原始 2 Hz 标签必须保留。



## 2\. 论文问题与实验边界

---





推荐题目：



> 基于段落层级上下文的时变音乐感知情绪预测
> 
> 



推荐英文题目：



> Section\-Hierarchical Modeling for Time\-Varying Music Emotion Prediction
> 
> 



输入和输出：



- 输入：一首完整歌曲或片段；

- 输出：从第 15 秒开始、每秒一个 `[valence, arousal]`；

- 主指标：Valence CCC、Arousal CCC；

- 辅助指标：MAE、RMSE；

- 统计单位：歌曲，而不是秒或切片。

首轮不做以下内容：



- 不生成自然语言情绪描述；

- 不训练 All\-In\-One；

- 不把 All\-In\-One 的输出称为人工结构真值；

- 不把 MusicCaps 当作 VA 监督；

- 不加入 GNN、LLM、歌词、和弦和图像生成；

- 不把不同数据集按歌曲 ID 强行拼接。

---



## 3\. 四个必须完成的模型



### B0：训练集均值



对所有时间点输出训练集 VA 均值。这是检查数据和指标的最低基线。



### B1：局部模型



输入当前秒及邻近秒的 MERT 特征，使用 MLP 或 1D\-CNN 回归 VA。它回答短时声学信息能做到什么程度。



### B2：平坦时序模型



将整首歌的秒级 MERT 特征直接送入双向 GRU，不输入任何乐段信息。这是最重要的强基线。



### B3：等长分段层级模型



设 All\-In\-One 为该曲产生 `K` 个有效乐段，但不用其边界，而是把有效时间轴机械切成 `K` 个等长段，再使用和主模型相同的层级网络。



### S1：自动乐段层级模型



1. 对每个自动乐段中的秒级 MERT 特征平均池化，得到 `z_k`；

2. 将乐段序列送入一层双向 GRU，得到上下文 `c_k`；

3. 对每秒拼接 `[h_t, z_k, c_k]`；

4. 输出逐秒 VA；

5. 可选地增加乐段平均 VA 辅助损失。

只有当 S1 同时稳定优于 B2 和 B3，论文才能主张“自动音乐乐段具有增量价值”。



---



## 4\. 推荐目录



```Plain Text
/home/<your-user>/mer-structure/
├── configs/
│   ├── b1.yaml
│   ├── b2.yaml
│   ├── b3.yaml
│   └── s1.yaml
├── data/
│   ├── raw/
│   │   └── deam/
│   │       ├── annotations/
│   │       └── audio_original/
│   ├── wav/
│   ├── labels_1hz/
│   ├── structures/
│   ├── features_mert95m/
│   ├── splits/
│   └── manifest.csv
├── src/
│   ├── prepare_deam.py
│   ├── validate_manifest.py
│   ├── extract_mert.py
│   ├── make_splits.py
│   ├── train.py
│   ├── evaluate.py
│   └── compare_models.py
├── runs/
├── reports/
├── env-locks/
└── README.md
```



---



## 5\. Ubuntu 与 NVIDIA GPU 环境



All\-In\-One 依赖 NATTEN（Neighborhood Attention）和 Demucs。Ubuntu 原生环境可以直接使用 Linux CUDA 工具链，适合本项目的批量结构分析和模型训练。



建议：



- 所有 Python、CUDA、FFmpeg 和训练命令都在 Ubuntu 终端执行；

- All\-In\-One 使用独立 Conda 环境 `aio`；

- MERT 和情绪模型使用独立环境 `mer`；

- 不在两个环境之间混装依赖；

- 项目路径使用英文字符，减少第三方工具处理中文路径时的问题。

如果服务器没有桌面环境也可以运行，All\-In\-One 的 `--visualize` 只会保存文件，不要求图形界面。



---



## 6\. 检查 Ubuntu、NVIDIA 驱动和基础工具



### 6\.1 检查系统和 GPU



```Bash
cat /etc/os-release
uname -a
nvidia-smi
```



`nvidia-smi` 必须能看到显卡、驱动版本和显存。NVIDIA 驱动由系统管理员或 Ubuntu 主机负责安装；如果该命令不存在或失败，应先修复驱动，再安装 Python 包。检查 CUDA 编译器：



```Bash
nvcc --version || true
```



运行预训练模型只需要驱动和对应的 PyTorch CUDA wheel；编译 NATTEN 时还需要 CUDA Toolkit 和 `nvcc`。



### 6\.2 安装基础软件



```Bash
sudo apt update
sudo apt install -y git git-lfs wget curl unzip ffmpeg build-essential cmake ninja-build pkg-config
ffmpeg -version
```



创建项目目录：



```Bash
mkdir -p "$HOME/mer-structure"
cd "$HOME/mer-structure"
mkdir -p configs data/raw/deam/annotations data/raw/deam/audio_original
data_dirs=(data/wav data/labels_1hz data/structures data/features_mert95m data/splits runs reports env-locks src)
mkdir -p "${data_dirs[@]}"
```



### 6\.3 安装 Miniforge



```Bash
cd /tmp
wget -O Miniforge3.sh https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh
bash Miniforge3.sh -b -p "$HOME/miniforge3"
source "$HOME/miniforge3/etc/profile.d/conda.sh"
conda init bash
```



重新打开 Ubuntu 终端后检查：



```Bash
conda --version
```



---



## 7\. 建立 All\-In\-One 环境



下面采用 Python 3\.10、PyTorch 2\.5\.0 CUDA 12\.1 和 NATTEN 0\.17\.5。原因是 All\-In\-One 1\.1\.0 使用旧的 `natten.functional.natten1dqkrpb` 等接口；不能不经测试直接换成 2026 年的最新 NATTEN。



```Bash
conda create -n aio python=3.10 -y
conda activate aio
python -m pip install --upgrade pip setuptools wheel

pip install torch==2.5.0+cu121 torchvision==0.20.0+cu121 torchaudio==2.5.0+cu121 \
  --extra-index-url https://download.pytorch.org/whl/cu121

pip install natten==0.17.5+torch250cu121 -f https://whl.natten.org
pip install cython "numpy<2"
pip install git+https://github.com/CPJKU/madmom
pip install allin1==1.1.0
```



验证：



```Bash
python -c "import torch; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
python -c "import natten; print(natten.__version__)"
python -c "import allin1; print('allin1 import ok')"
allin1 -h
```



如果显卡驱动不支持 CUDA 12\.1，先根据 PyTorch 和 NATTEN 的官方 wheel 对照表选择一组**\*\*完全匹配\*\***的版本，不要只替换其中一个包。



安装成功后立即冻结环境：



```Bash
mkdir -p env-locks
pip freeze > env-locks/aio-pip-freeze.txt
python -c "import torch, natten; print(torch.__version__, natten.__version__)" \
  > env-locks/aio-core-versions.txt
```



---



## 8\. 建立 MERT 与训练环境



```Bash
conda create -n mer python=3.10 -y
conda activate mer
python -m pip install --upgrade pip setuptools wheel

pip install torch==2.5.0+cu121 torchaudio==2.5.0+cu121 \
  --extra-index-url https://download.pytorch.org/whl/cu121
pip install transformers==4.38.2 accelerate==0.28.0
pip install pandas==2.2.3 numpy==1.26.4 scipy==1.13.1 scikit-learn==1.5.2
pip install librosa==0.10.2.post1 soundfile==0.12.1 pyyaml==6.0.2
pip install matplotlib==3.9.2 seaborn==0.13.2 tqdm==4.66.5
```



检查 GPU 并下载 MERT 权重：



```Bash
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
python - <<'PY'
from transformers import AutoModel, Wav2Vec2FeatureExtractor
name = "m-a-p/MERT-v1-95M"
Wav2Vec2FeatureExtractor.from_pretrained(name, trust_remote_code=True)
AutoModel.from_pretrained(name, trust_remote_code=True)
print("MERT download and load ok")
PY
```



MERT\-v1\-95M 的输入采样率是 24 kHz，输出约 75 帧/秒，共 13 层表示（卷积输出加 12 个 Transformer 层），每帧 768 维。首轮建议固定使用最后四层平均，再按一秒区间平均为 `[T, 768]`；层选择只能在验证集上改变。



```Bash
pip freeze > env-locks/mer-pip-freeze.txt
```



---



## 9\. 获取 DEAM



### 9\.1 下载官方动态标注



标注可以从 Zenodo 记录 `11400122` 下载。以下命令只下载约 4\.7 MB 的标注，不包含完整音频：



```Bash
cd "$HOME/mer-structure"
wget -O /tmp/DEAM_Annotations.zip \
  https://zenodo.org/api/records/11400122/files/DEAM_Annotations.zip/content
unzip -q /tmp/DEAM_Annotations.zip -d data/raw/deam/annotations
find data/raw/deam/annotations -type f | sort
```



主任务只需要平均动态标注中的两个文件：



```Plain Text
annotations/annotations averaged per song/dynamic (per second annotations)/arousal.csv
annotations/annotations averaged per song/dynamic (per second annotations)/valence.csv
```



检查真实列名：



```Bash
python - <<'PY'
from pathlib import Path
import pandas as pd
root = Path("data/raw/deam/annotations")
a = next(root.rglob("arousal.csv"))
v = next(root.rglob("valence.csv"))
for p in (a, v):
    df = pd.read_csv(p)
    print(p)
    print("shape:", df.shape)
    print("first columns:", df.columns[:6].tolist())
    print("song ids:", df.song_id.head().tolist())
PY
```



预期前几列是：



```Plain Text
song_id, sample_15000ms, sample_15500ms, sample_16000ms, ...
```



这一步同时证明原始平均动态标签是 2 Hz 且从 15 秒开始。



### 9\.2 获取音频



DEAM 官方页将“annotations”“Creative Commons audio”“完整歌曲访问”分开提供。由于音频许可和可访问范围可能变化，应通过官方页面按当前条款下载：



\- 官方页：\<https://cvml\.unige\.ch/databases/DEAM/\>

\- 标注镜像：\<https://zenodo\.org/records/11400122\>



将获得且许可允许研究使用的音频放入：



```Plain Text
data/raw/deam/audio_original/<song_id>.<扩展名>
```



例如 `2.mp3`、`3.mp3`。不要把未获得的商业完整歌曲从非官方来源抓取，也不要将受限音频提交到 Git。最终可用样本数以“音频存在 \+ 动态标签存在 \+ 时间轴通过质检”的交集为准，不要在论文中预先承诺恰好 1,802 首。



检查音频 ID 和标注 ID 交集：



```Bash
conda activate mer
python src/prepare_deam.py \
  --audio-dir data/raw/deam/audio_original \
  --annotation-root data/raw/deam/annotations \
  --inspect-only
```



`prepare_deam.py` 是本项目需要实现的脚本，接口和验收标准见第 13 节。当前 `lunwen` 目录只有研究文档，没有这套实验代码；不要把尚未创建的脚本命令误认为已经跑通。



---



## 10\. 统一音频格式



All\-In\-One 官方建议先转 WAV，以避免不同 MP3 解码器带来约几十毫秒偏移。统一为单声道、24 kHz、PCM 16\-bit WAV；All\-In\-One 和 MERT 必须读取同一个派生 WAV。



单曲转换：



```Bash
ffmpeg -y -i data/raw/deam/audio_original/2.mp3 \
  -ac 1 -ar 24000 -c:a pcm_s16le data/wav/2.wav
ffprobe -v error -show_entries format=duration \
  -of default=noprint_wrappers=1:nokey=1 data/wav/2.wav
```



批量转换应由 `prepare_deam.py` 调用 `ffmpeg`，并把探测到的实际时长、采样率和状态写入 `manifest.csv`。不要手工写死歌曲时长。



---



## 11\. 先跑一首歌的结构分析



确认 `data/wav/2.wav` 存在后：



```Bash
conda activate aio
cd "$HOME/mer-structure"
allin1 data/wav/2.wav \
  --out-dir data/structures \
  --device cuda \
  --visualize \
  --viz-dir reports/structure-smoke
```



检查结果：



```Bash
python -m json.tool data/structures/2.json | head -80
ls -lh data/structures/2.json reports/structure-smoke/
```



JSON 应至少包含：



```JSON
{
  "path": "data/wav/2.wav",
  "bpm": 120,
  "beats": [],
  "downbeats": [],
  "segments": [
    {"start": 0.0, "end": 12.3, "label": "intro"}
  ]
}
```



验收条件：



- `segments` 非空；

- 每段满足 `0 <= start < end <= duration + 0.1`；

- 相邻段按时间递增；

- JSON 能被 Python 正常读取；

- 可视化中边界没有整体偏移 15 秒。

注意：DEAM 标签从 15 秒开始，不代表音频和 All\-In\-One 也要裁掉前 15 秒。结构分析始终处理完整音频；训练时只对有标签的时间区间计算损失。



批处理：



```Bash
conda activate aio
allin1 'data/wav/*.wav' \
  --out-dir data/structures \
  --device cuda \
  --no-multiprocess
```



All\-In\-One 会跳过已有 JSON，可以断点续跑。建议先处理 5 首，再处理全部。`harmonix-all` 是 8 模型集成，速度慢时可先用 `--model harmonix-fold0` 做工程测试；正式实验必须固定一种模型并记录版本，不能混用 fold0 和 ensemble 结果。



批处理完成后：



```Bash
find data/wav -maxdepth 1 -name '*.wav' | wc -l
find data/structures -maxdepth 1 -name '*.json' | wc -l
```



数量不一致时必须列出失败歌曲，不能静默丢弃。



---



## 12\. 先跑一首歌的 MERT 特征



正式提取由 `extract_mert.py` 完成：



```Bash
conda activate mer
python src/extract_mert.py \
  --manifest data/manifest.csv \
  --output-dir data/features_mert95m \
  --model m-a-p/MERT-v1-95M \
  --layer-mode mean-last-4 \
  --chunk-sec 30 \
  --overlap-sec 2 \
  --target-hz 1 \
  --device cuda \
  --track-id 2
```



长音频不能一次无上限送入模型。建议实现以下确定性规则：



1. 读取 24 kHz 单声道 WAV；

2. 使用 30 秒窗口、左右 2 秒重叠；

3. 每个窗口单独推理，丢弃重叠区中靠近窗口边缘的一份特征；

4. 拼回绝对音频时间轴；

5. 取最后四层隐状态平均；

6. 按 `[t, t+1)` 平均为 1 Hz；

7. 保存 `features`、`time_start_sec`、`time_end_sec`、模型名、层策略和代码版本。

建议保存为：



```Python
{
    "track_id": "2",
    "features": FloatTensor[T, 768],
    "time_start_sec": FloatTensor[T],
    "time_end_sec": FloatTensor[T],
    "model_name": "m-a-p/MERT-v1-95M",
    "layer_mode": "mean-last-4",
    "sample_rate": 24000
}
```



单曲验收：



```Bash
python - <<'PY'
import torch
x = torch.load("data/features_mert95m/2.pt", map_location="cpu", weights_only=False)
print(x["features"].shape)
print(x["time_start_sec"][:5], x["time_end_sec"][:5])
print(torch.isfinite(x["features"]).all().item())
PY
```



批量运行：



```Bash
python src/extract_mert.py \
  --manifest data/manifest.csv \
  --output-dir data/features_mert95m \
  --model m-a-p/MERT-v1-95M \
  --layer-mode mean-last-4 \
  --chunk-sec 30 \
  --overlap-sec 2 \
  --target-hz 1 \
  --device cuda \
  --resume
```



建议保存 float16 特征以降低磁盘占用，但训练加载后转回 float32。提取阶段使用 `model.eval()` 和 `torch.inference_mode()`，不得产生梯度。



---



## 13\. 必须实现的数据脚本



当前工作区只有论文研究文档，没有可直接训练的项目源码。为了让第 9、12、15 节的命令真正可执行，至少需要实现以下脚本。应先实现数据闭环，再实现模型。



### 13\.1 `prepare_deam.py`



输入：音频目录、官方 `arousal.csv`、`valence.csv`。输出：统一 WAV、每曲标签 CSV 和 `manifest.csv`。



处理规则：



1. 用 `song_id` 连接 arousal 和 valence，不按行号连接；

2. 从列名正则 `sample_(\d+)ms` 解析真实时间；

3. 将空字符串和 NaN 标为无效；

4. 只保留 V/A 同时有效的点；

5. 2 Hz 转 1 Hz 时，只合并同一个 `[t,t+1)` 内的两个有效点；

6. 结果时间写为该区间中心 `t+0.5`；

7. 不把 15 秒减成 0 秒；

8. 用 FFprobe 读取真实音频时长；

9. 超出音频时长的标签设为无效并记录警告；

10. 使用固定随机种子建立曲目级划分。

每曲 `labels_1hz/<id>.csv`：



```Plain Text
time_sec,valence,arousal,valid
15.5,0.12,0.35,1
16.5,0.10,0.39,1
```



### 13\.2 `validate_manifest.py`



必须检查：



- 曲目 ID 唯一；

- WAV、标签、结构和特征文件存在；

- 标签时间严格递增；

- VA 在 `[-1, 1]` 或实际记录的官方范围内；

- 标签末端不超过音频时长容差；

- 结构段合法且覆盖范围可解释；

- 特征时间区间与 WAV 对齐；

- 同一曲目只属于一个划分；

- 训练、验证、测试没有相同 ID；

- 排除歌曲有明确 `exclude_reason`。

运行：



```Bash
python src/validate_manifest.py \
  --manifest data/manifest.csv \
  --require labels,structures,features \
  --report reports/data-validation.json
```



只有返回码为 0 才进入训练。



### 13\.3 `make_splits.py`



推荐先固定 `70%/15%/15%` 曲目级划分，种子 20260903：



```Bash
python src/make_splits.py \
  --manifest data/manifest.csv \
  --ratios 0.70 0.15 0.15 \
  --seed 20260903 \
  --out-dir data/splits
```



如果元数据足够，应按来源类别或曲目时长分层；同一作品的版本、重复片段或近重复音频必须放入同一划分。划分生成后冻结为文本文件，所有模型复用。



---



## 14\. 时间轴如何严格对齐



每个训练样本应以绝对秒为准，而不是数组下标。对第 `i` 个 1 Hz 标签：



```Plain Text
标签区间：[15+i, 16+i)
标签时间：15.5+i
MERT 特征：同一区间内约 75 个帧取平均
结构归属：区间中心 15.5+i 落入哪个 [segment.start, segment.end)
```



最后一个乐段可使用闭区间终点，其余段使用左闭右开：



```Plain Text
[start, end)
```



边界恰好等于时间中心时归入新段。无有效标签的秒保留 mask，但不进入损失和指标。



反应时延不能在测试集逐曲调优。可在验证集比较全局延迟 `0,1,2,3,4,5` 秒，选择一个后冻结：



```Bash
for lag in 0 1 2 3 4 5; do
  python src/train.py --config configs/b2.yaml --label-lag-sec "$lag" --seed 20260903
done
```



时间延迟的正负方向必须在代码和论文中明确。例如 `lag=2` 定义为“将第 t 秒人工标签与第 t\-2 秒音频证据对应”。



---



## 15\. 训练顺序和命令



### 15\.1 先生成配置



四个配置除模型类型外，应共享：



- 同一 manifest 和划分；

- 输入维度 768；

- 隐藏维度和参数量尽量接近；

- AdamW；

- 初始学习率 `1e-3`；

- weight decay `1e-4`；

- 梯度裁剪 `1.0`；

- 最多 100 epoch；

- 验证集 CCC 早停，patience 12；

- batch 内按歌曲 padding，并使用有效 mask；

- 每首歌先计算损失，再对歌曲平均。

初始损失：



```Plain Text
L_second = (1 - CCC_valence) + (1 - CCC_arousal)
           + 0.5 * (MAE_valence + MAE_arousal)
L_total  = L_second + alpha * L_segment
```



`alpha` 只在验证集从 `{0, 0.1, 0.3, 1.0}` 选择。



### 15\.2 冒烟训练



先用 8 首训练、2 首验证，运行 2 个 epoch：



```Bash
conda activate mer
python src/train.py \
  --config configs/b2.yaml \
  --seed 20260903 \
  --max-tracks 10 \
  --max-epochs 2 \
  --run-name smoke-b2
```



验收：loss 有限、能反向传播、保存 checkpoint、验证输出形状与标签一致。然后对 S1 做同样测试：



```Bash
python src/train.py \
  --config configs/s1.yaml \
  --seed 20260903 \
  --max-tracks 10 \
  --max-epochs 2 \
  --run-name smoke-s1
```



### 15\.3 正式训练



先只跑一个种子完成全链路：



```Bash
for model in b0 b1 b2 b3 s1; do
  python src/train.py \
    --config "configs/${model}.yaml" \
    --seed 20260903 \
    --run-name "${model}-seed20260903"
done
```



确认 B2 优于 B0 后，再运行至少 3 个种子：



```Bash
for seed in 20260903 20260904 20260905; do
  for model in b1 b2 b3 s1; do
    python src/train.py \
      --config "configs/${model}.yaml" \
      --seed "$seed" \
      --run-name "${model}-seed${seed}"
  done
done
```



最终论文建议 5 个种子，但应在模型设计和超参数冻结后再增加 `20260906`、`20260907`。



### 15\.4 必做消融



```Bash
python src/train.py --config configs/s1.yaml --alpha 0 \
  --seed 20260903 --run-name s1-no-segment-loss

python src/train.py --config configs/s1.yaml --disable-segment-gru \
  --seed 20260903 --run-name s1-no-segment-gru

python src/train.py --config configs/s1.yaml --ignore-functional-labels \
  --seed 20260903 --run-name s1-boundaries-only
```



主模型建议默认忽略 `intro/verse/chorus` 名称，只使用边界。功能标签实验作为辅助消融，因为不同风格不一定适合流行曲式类别。



---



## 16\. 评价与统计命令



每次运行都要保存：



- 每曲真实 VA 和预测 VA；

- 每曲 CCC、MAE、RMSE；

- 自动边界；

- 配置文件副本；

- 随机种子；

- Python 包版本；

- 最佳 epoch 和 checkpoint；

- Git commit；若尚未使用 Git，则至少保存源码哈希和时间戳。

测试：



```Bash
python src/evaluate.py \
  --run-dir runs/s1-seed20260903 \
  --split test \
  --save-predictions \
  --out reports/s1-seed20260903-test.json
```



比较 S1、B2 和 B3：



```Bash
python src/compare_models.py \
  --runs 'runs/b2-seed*' 'runs/b3-seed*' 'runs/s1-seed*' \
  --unit track \
  --bootstrap 10000 \
  --paired-permutations 10000 \
  --holm valence_ccc arousal_ccc \
  --out reports/main-comparison.json
```



CCC（Concordance Correlation Coefficient，一致性相关系数）必须按每曲先计算，再对曲目汇总。不能把所有测试秒拼成一个超长向量作为唯一主结果，也不能把每秒当成独立样本做显著性检验。



还要报告：



- 各指标跨种子的均值和标准差；

- S1\-B2、S1\-B3 的曲目级配对差；

- 95% 曲目级 bootstrap 置信区间；

- 边界附近与段内位置的分层结果；

- 失败歌曲和排除原因。

边界分析可预注册距离组：`0-1 s`、`1-3 s`、`3-5 s`、`>5 s`，也应把到最近边界距离作为连续变量分析，避免只挑最有利窗口。



---



## 17\. 最小人工工作



从冻结测试集中分层抽取 60–100 首，至少两名标注者独立审计：



- 主要段落边界时间；

- 边界显著程度；

- 可选功能类别；

- 不确定区间；

- 是否不适合 intro/verse/chorus 体系。

人工审计应在不知道 S1 与 B2 结果的情况下完成。它用于诊断 All\-In\-One 结构质量，不把 60–100 首审计扩写成全 DEAM 人工结构数据集。



可以增加边界扰动敏感性：



- 每个边界随机偏移 `±1 s`、`±3 s`；

- 随机删除 10%、30% 边界；

- 使用人工审计修订边界；

- 与原始自动边界比较情绪预测变化。

---



## 18\. PMEmo、SALAMI 应放在哪里



### PMEmo



PMEmo 可用于外部动态情绪验证，但不能和 DEAM 按歌曲直接合并。它的动态标签频率、片段范围、音频可用性和许可需要单独核验。只有 DEAM 主实验完成后再加入。



### SALAMI



SALAMI 用于评价或审计结构分析器，不提供与 DEAM 同曲对齐的动态 VA。它不能替代 DEAM 训练情绪模型，也不能把 SALAMI 的结构标签复制给 DEAM 曲目。



推荐顺序：



1. DEAM \+ MERT 跑通 B0/B1/B2；

2. DEAM \+ All\-In\-One 跑通 B3/S1；

3. 完成消融和统计；

4. 用 SALAMI 或人工子集诊断结构质量；

5. 条件允许时用 PMEmo 做外部验证。

---



## 19\. 每阶段验收门槛



### 阶段 A：环境



- `nvidia-smi` 正常；

- `allin1 -h` 正常；

- MERT 能下载并加载；

- 两个环境的 `pip freeze` 已保存。

### 阶段 B：单曲数据闭环



- WAV 可播放且时长正确；

- 1 Hz 标签从 15\.5 秒开始；

- All\-In\-One JSON 合法；

- MERT 特征为 `[T,768]` 且无 NaN；

- 同一绝对秒能找到标签、特征和乐段归属。

### 阶段 C：小样本训练



- B2 和 S1 各能完成 2 epoch；

- mask 后的 loss 有限；

- checkpoint 可重新加载并生成相同预测；

- B0 指标已保存。

### 阶段 D：正式实验



- B2 稳定优于 B0；

- 所有模型共享相同划分；

- 至少 3 个种子；

- 测试集只在设计冻结后评估；

- 每曲预测和统计结果可追溯。

### Go/No\-Go



继续以“结构有帮助”为主线需要：



- S1 相对 B2 在多个种子方向一致；

- S1 相对 B3 仍有改善；

- 置信区间和曲目级分析不由少数异常歌曲驱动；

- 结构边界质量没有普遍失效。

若 S1 只优于 B2、不优于 B3，结论应改为“层级分组有效，但音乐段落本身未显示额外价值”。若 S1 不优于 B2，应报告负结果和结构质量敏感性，不继续堆叠复杂模块掩盖主假设失败。



---



## 20\. 常见问题



### `ModuleNotFoundError` 或找不到旧 NATTEN 函数



原因通常是安装了最新 NATTEN，而 All\-In\-One 1\.1\.0 使用旧接口。重新创建 `aio` 环境并使用与 PyTorch 精确匹配的 NATTEN 0\.17\.5 wheel。不要在原环境反复覆盖安装多个 Torch。



### `torch.cuda.is_available()` 为 `False`



先在 Ubuntu 主机更新 NVIDIA 驱动并确认 `nvidia-smi` 正常，再检查 CUDA Toolkit。确认安装的是 CUDA wheel，不是 CPU wheel。



### All\-In\-One 显存不足



先用单模型：



```Bash
allin1 data/wav/2.wav --model harmonix-fold0 --device cuda
```



正式实验若改用 fold0，则所有歌曲必须统一使用 fold0 并在论文记录。也可以关闭可视化、逐曲运行并清理 Demucs 中间文件。



### MP3 与标签整体错位



确保 All\-In\-One、MERT 和时长探测全部读取同一个转换后的 WAV。不要一个模块读 MP3、另一个模块读 WAV。



### 第一条标签被放在 0 秒



这是解析错误。必须从列名 `sample_15000ms` 得到绝对时间 15\.0 秒，不能按列序号从零生成时间。



### CCC 出现 NaN



短序列或常量序列方差为零时 CCC 不可定义。训练时使用带 epsilon 的稳定实现；评估时记录不可定义歌曲，不应悄悄替换为 0。检查模型是否退化为常量输出。



### MERT 整曲推理显存溢出



使用固定窗口和重叠拼接，不要降低采样率到非 24 kHz。提取完成后缓存秒级特征，训练期间不再调用 MERT。



---



## 21\. 推荐执行清单



按以下顺序执行，不要直接从第 1 步跳到全量训练：



1. 安装 Ubuntu 基础工具、Miniforge 并验证 NVIDIA GPU；

2. 建立独立 `aio` 和 `mer` 环境；

3. 下载 DEAM 标注，通过官方渠道获取可用音频；

4. 实现 `prepare_deam.py`，核对原始 2 Hz 和 15 秒起点；

5. 转换一首 WAV；

6. 对一首歌运行 All\-In\-One；

7. 实现 `extract_mert.py`，对同一首歌提取 1 Hz 特征；

8. 实现 `validate_manifest.py` 并通过单曲校验；

9. 扩展到 5–10 首，运行 2 epoch 冒烟训练；

10. 批量生成结构和 MERT 特征；

11. 固定曲目级划分；

12. 先完成 B0、B1、B2；

13. 确认 B2 稳定后实现 B3、S1；

14. 运行 3–5 个种子、消融和曲目级统计；

15. 完成人工结构审计、边界敏感性和论文写作。

---



## 22\. 来源与版本记录



\- DEAM 官方页：\<https://cvml\.unige\.ch/databases/DEAM/\>

\- DEAM 标注 Zenodo：\<https://zenodo\.org/records/11400122\>

\- All\-In\-One：\<https://github\.com/mir\-aidj/all\-in\-one\>

\- All\-In\-One PyPI 1\.1\.0：\<https://pypi\.org/project/allin1/\>

\- MERT 官方仓库：\<https://github\.com/yizhilll/MERT\>

\- MERT\-v1\-95M：\<https://huggingface\.co/m\-a\-p/MERT\-v1\-95M\>

\- NATTEN 安装说明：\<https://natten\.org/install/\>



论文引用时至少引用 DEAM、All\-In\-One 和 MERT 的原始论文，并在附录中记录：下载日期、数据许可、软件版本、模型 checkpoint、数据排除规则、划分文件和随机种子。



---



## 23\. DEAM 下载完成后的立即操作



当前数据目录为 `/music-emotion-project/data`，其中已经存在：



```Plain Text
/music-emotion-project/data/
├── annotations_raw/
├── audio_raw/
├── DEAM_audio/
└── DEAM_Annotations.zip
```



下面所有命令都在服务器 Ubuntu 终端执行。先进入项目目录：



```Bash
cd /music-emotion-project
mkdir -p data/labels_1hz data/wav data/structures data/features_mert95m
mkdir -p data/splits reports env-locks src runs
```



### 23\.1 检查下载结果



```Bash
find data -maxdepth 3 -type f | sort | sed -n '1,100p'
find data/audio_raw -type f | wc -l
find data/DEAM_audio -type f | wc -l
find data/annotations_raw -type f | wc -l
```



检查音频扩展名和标注文件：



```Bash
find data/audio_raw data/DEAM_audio -type f | sed 's/.*\.//' | sort | uniq -c
find data/annotations_raw -type f | grep -Ei 'arousal|valence|\.csv$' | sed -n '1,50p'
```



### 23\.2 解开并确认官方标注



如果 `annotations_raw` 还没有 `arousal.csv` 和 `valence.csv`，执行：



```Bash
unzip -q -o data/DEAM_Annotations.zip -d data/annotations_raw
```



定位实际文件：



```Bash
find data/annotations_raw -type f \( -iname 'arousal.csv' -o -iname 'valence.csv' \) -print
```



读取列名和曲目数量：



```Bash
conda activate mer
python - <<'PY'
from pathlib import Path
import pandas as pd

root = Path("data/annotations_raw")
for name in ("arousal.csv", "valence.csv"):
    paths = list(root.rglob(name))
    if not paths:
        raise FileNotFoundError(name)
    path = paths[0]
    df = pd.read_csv(path)
    print("FILE:", path)
    print("SHAPE:", df.shape)
    print("FIRST_COLUMNS:", df.columns[:8].tolist())
    print("SONG_COUNT:", df["song_id"].nunique())
    print("FIRST_IDS:", df["song_id"].head().tolist())
PY
```



预期结果应包含：



```Plain Text
sample_15000ms
sample_15500ms
sample_16000ms
```



这说明动态标注从音频绝对时间 15 秒开始，原始时间间隔为 500 ms。不要把第一列重新编号为 0 秒。



### 23\.3 确认音频目录和曲目编号



先比较两个音频目录中的文件名，不要立即复制或删除任何目录：



```Bash
python - <<'PY'
from pathlib import Path

for dirname in ("data/audio_raw", "data/DEAM_audio"):
    root = Path(dirname)
    files = sorted(p for p in root.rglob("*") if p.is_file())
    ids = {p.stem for p in files}
    print(dirname, "files=", len(files), "sample=", [p.name for p in files[:5]])
    print("stem sample=", sorted(ids)[:10])
PY
```



DEAM 音频文件可能是 MP3，也可能已经是 WAV。后续必须以“实际能被 FFmpeg 解码的文件”为准。将有用音频统一放在 `data/audio_original`：



```Bash
mkdir -p data/audio_original
```

*\# 只有在确认目录内容后执行；下面命令不会覆盖同名文件*

find data/DEAM\_audio \-type f \-exec cp \-n \{\} data/audio\_original/ \\;

```Plain Text

如果 `audio_raw` 才是完整音频目录，应改为：

```bash
find data/audio_raw -type f -exec cp -n {} data/audio_original/ \;
```



复制后检查：



```Bash
find data/audio_original -type f | wc -l
find data/audio_original -type f | sed -n '1,10p'
```



### 23\.4 先转换 1 首 WAV



先选一个同时存在动态标注和音频的 ID，例如 `2`。如果文件不是 `2.mp3`，先用 `find` 找到对应文件名：



```Bash
find data/audio_original -type f \( -iname '2.mp3' -o -iname '2.wav' -o -iname '2.flac' \)
```



转换为单声道、24 kHz、PCM WAV：



```Bash
ffmpeg -y -i data/audio_original/2.mp3 \
  -ac 1 -ar 24000 -c:a pcm_s16le data/wav/2.wav
ffprobe -v error -show_entries format=duration \
  -of default=noprint_wrappers=1:nokey=1 data/wav/2.wav
```



如果实际文件是 WAV 或 FLAC，只替换输入文件路径。确认音频能解码后，再编写批量处理脚本；不要手工为 1,802 首歌曲逐条写命令。



### 23\.5 当前阶段的完成标准



完成下面四项后，再进行 All\-In\-One 和 MERT：



```Plain Text
[ ] arousal.csv 和 valence.csv 均能读取
[ ] 两个文件均存在 song_id 和 sample_15000ms
[ ] 已确认使用哪个音频目录，且音频数量已记录
[ ] 至少一首音频已成功转换为 data/wav/<id>.wav
```



下一步顺序为：



```Plain Text
1. 实现 prepare_deam.py，生成 labels_1hz/ 和 manifest.csv
2. 批量转换 WAV 并运行 validate_manifest.py
3. 用 aio 环境运行 All-In-One，生成 structures/
4. 用 mer 环境运行 MERT，生成 features_mert95m/
5. 单曲对齐检查通过后，再实现 B0/B1/B2 训练
```





